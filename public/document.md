# File Hash API

## Overview

This API provides endpoints to generate cryptographic hash values for file contents.

## Features

- Multiple hash algorithms support
- File metadata retrieval
- Batch processing capability
- Security measures against path traversal attacks

## Usage

Send a GET request to `/api/hash` with the file name as a query parameter.
